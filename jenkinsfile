pipeline{
	agent any 
	stages{
		stage('number 1 stage'){
			steps {
				echo "my name is annick"
                sh 'ps -ef'
                sh 'sudo systemctl status jenkins'
                sh 'sudo chmod +x annick.sh'
                sh 'bash -x annick.sh'
			}
		}
	}
}
}
